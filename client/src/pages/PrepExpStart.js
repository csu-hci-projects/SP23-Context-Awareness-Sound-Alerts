import {useState} from "react";
import {alertConfig} from "../components/alertConfig";

export default function PrepExpStart({context}){

    return(
        <div className={"content"}>
            <h1>About to Begin Experiment, Press Button to Start</h1>
        </div>
    )
}